{
"scripts": {
  "migrate": "knex migrate:latest && knex seed:run",
  "start": "npm run migrate && node index.js"
},
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "knex": "^3.1.0",
    "morgan": "^1.10.0",
    "pg": "^8.12.0"
  },
  "engines": {
    "node": "20.x"
  }
}